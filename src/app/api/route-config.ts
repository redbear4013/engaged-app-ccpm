// Global route configuration for all API routes
// This prevents build-time static generation which causes environment variable errors

export const dynamic = 'force-dynamic'
export const runtime = 'nodejs'
